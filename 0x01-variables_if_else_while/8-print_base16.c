#include <stdio.h>

/**
 * main -Entry point
 *
 * Return: 0 Success
 */
int main(void)
{
	int a;
	char alph;

	for (a = '0'; a <= '9'; a++)
		putchar(d);
	for (alph = 'a'; alph <= 'f'; alph++)
		putchar(alph);
	putchar('\n');
	return (0);
}
